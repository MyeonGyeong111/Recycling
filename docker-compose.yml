version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
      - ./frontend/dist:/app/frontend/dist
    environment:
      - ENVIRONMENT=development
